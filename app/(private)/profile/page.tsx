import { ProfilePage } from '@/app/(private)/profile/components/ProfilePage/ProfilePage';

export const metadata = {
  title: 'Profile | BrainFlip',
  description: 'Profile BrainFlip',
};

const Profile = () => {
  return <ProfilePage />;
};

export default Profile;
